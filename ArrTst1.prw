#INCLUDE 'PROTHEUS.CH'

User Function ARRTST()

    Local cNome  := 'Leonardo'
    Local cIdade := '35'
    Local cEnd   := 'Rua Torta'
    Local cNum   := '10'

    MsgInfo('Nome: ' + cNome + CRLF + 'Idade: ' + cIdade + CRLF + 'Endereço: ' + cEnd + ', ' + cNum)

Return
